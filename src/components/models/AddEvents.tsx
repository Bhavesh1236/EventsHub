import { View, Text } from 'react-native'
import React from 'react'
import AppView from '../shared/AppView'
import AppText from '../shared/AppText'

const AddEvents = () => {
  return (
    <AppView>
      <AppText>AddEvents</AppText>
    </AppView>
  )
}

export default AddEvents